# Environments Directory

This directory contains environment-specific configurations and deployment manifests.

## Structure

- `development/` - Local development configurations
- `staging/` - Staging environment settings
- `production/` - Production deployment configurations

Each environment directory will contain Kubernetes manifests, Helm values, and environment variables.