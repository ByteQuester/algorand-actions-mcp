✦ I have so far done the following for task 1:

   * Created the .github/workflows directory.
   * Created the ci.yml file with the CI/CD workflow.
   * Created the pr-validation.yml file with the PR validation workflow.
   * Added lint, typecheck, and test scripts to all package.json files in the
     monorepo.
   * Installed biome as a dev dependency in the root package.json.
   * Created a biome.json configuration file.
   * Fixed all the linting errors in the codebase.

  The next steps are to:

   * Fix the remaining linting errors in the remote-mcp-worker package.
   * Run the tests to ensure that all the changes are working as expected.
   * Commit the changes and create a pull request.