{
  "name": "@algorand-showcase/config",
  "version": "0.1.0",
  "description": "Configuration management utilities for Algorand MCP Workers",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "node scripts/dev/network-config-helper.js test",
    "setup-dev": "cp config/.env.example .env || echo '.env already exists'",
    "validate": "npm run type-check"
  },
  "dependencies": {
    "@algorand-showcase/types": "workspace:*",
    "zod": "^3.23.8"
  }
}