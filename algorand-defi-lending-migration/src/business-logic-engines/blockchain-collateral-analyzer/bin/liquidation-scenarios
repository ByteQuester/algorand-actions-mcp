#!/usr/bin/env python3
"""
CLI launcher for liquidation scenarios engine.
"""

import sys
import os

# Add the parent directory to the path
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from cli.liquidation_scenarios import main

if __name__ == "__main__":
    sys.exit(main())