# Apps Directory

This directory contains deployable applications for the Algorand Showcase project.

## Future Structure

- `actions-mcp-worker/` - Cloudflare Worker for GitHub Actions MCP server
- `remote-mcp-worker/` - Cloudflare Worker for remote Algorand operations

Each app will have its own deployment configuration and environment settings.